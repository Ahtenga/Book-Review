<!DOCTYPE html>
<html lang="en">
<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link rel="stylesheet" href="/styles/layout.css">
     <link rel="stylesheet" href="/styles/content.css">
     <title>Blog</title>
</head>
<body>
     <header class="navbar">
          <nav class="container">
               <div class="item-left">
                    <p>Book reviews</p>
               </div>
               <div class="item-right">
                    <% const p = typeof page === 'string' ? page : 'home'; %>
                    
                    <% if (p !== 'home') { %>
                      <a href="/">Home</a>
                    <% } %>

                    <% if (p !== 'create') { %>
                      <a href="/create">Create review</a>
                    <% } %>

                    <% if (p !== 'edit' && p !== 'edit-one') { %>
                      <a href="/edit">Edit reviews</a>
                    <% } %>

                    <!-- <% if (p === 'review' && review && review.id) { %>
                      <a href="/edit/<%= review.id %>">Edit this</a>
                    <% } %> -->
               </div>
          </nav>
     </header>
     <main class="content">